---
title: Review Report — P Phase
date: 2026-04-05
type: report
status: active
author: 審察者
phase: P (Plan)
target: doc/plans/plan_implementation_20260405.md
verdict: ✅ Pass（第 2 輪）
tags:
  - review
  - p-phase
---

# Review Report — P Phase

> 審察者獨立審查。對照 [[system_overview]]、[[data_schema]]、[[ui_spec]]。
> 審查對象：[[plan_implementation_20260405]]

---

## 判定：❌ Fail

**理由：** 存在 1 項 Critical 級設計缺陷（音訊串流單位與即時轉錄的矛盾），會從根本上破壞「即時」核心承諾。另有 4 項 Major 級遺漏，涉及 specs 定義的必要功能。整體架構與分期合理，修正後可 Re-review 通過。

---

## 1. 與 Specs 一致性

### 1.1 模組覆蓋（✅ 完整）

計畫涵蓋 [[system_overview#3.2 模組職責]] 定義的所有 11 個模組，無遺漏：

| Spec 模組 | 計畫位置 | 狀態 |
|-----------|---------|------|
| AudioRecorder | Phase 1-3 | ✅ |
| AudioImporter | Phase 1-4 | ✅ |
| StreamProcessor | Phase 3-1 | ✅ |
| Transcriber | Phase 1-2 | ✅ |
| KnowledgeBase | Phase 1-1 | ✅ |
| RAGCorrector | Phase 2-1 | ✅ |
| Summarizer | Phase 2-2 | ✅ |
| SessionManager | Phase 2-3 | ✅ |
| Exporter | Phase 2-4 | ✅ |
| FeedbackStore | Phase 1-5 | ✅ |
| ConfigManager | Phase 0-2 | ✅ |

### 1.2 UI 頁面覆蓋（✅ 完整）

| Spec 頁面 | 計畫位置 | 狀態 |
|-----------|---------|------|
| main_view（導航 + 狀態列） | Phase 4-1 | ✅ |
| dashboard_view（儀表板 + 編輯） | Phase 4-2 | ✅ |
| settings_view | Phase 4-3 | ✅ |
| terms_view | Phase 5-1 | ✅ |
| feedback_view | Phase 5-2 | ✅ |

### 1.3 資料模型覆蓋（⚠️ 部分省略）

Phase 0-1 列出 `TranscriptSegment`、`Correction`、`CorrectedSegment`、`ActionItem`、`SummaryResult` 的完整欄位。`Session`、`Participant`、`UserEdits`、`FeedbackEntry`、`SessionFeedback` 以「同理」帶過。可接受（偽代碼不必逐字展開），但實作時需確保完全對齊 [[data_schema]]。

### 1.4 目錄結構一致性（✅）

計畫新增 `app/core/models.py`，specs 目錄結構已更新（`ff0b75d` commit）。一致。

---

## 2. 完整性：分期與依賴

### 2.1 六期分期（✅ 合理）

```
Phase 0（基礎）→ Phase 1（獨立核心）→ Phase 2（組合）→ Phase 3（管線）→ Phase 4（UI 核心）→ Phase 5（UI 輔助 + 打包）
```

- 依賴方向正確：0 → 1 → 2 → 3 → 4 → 5
- Phase 1 五模組互不依賴，可平行，標記正確
- Phase 2 依賴 Phase 1 + data_models，正確
- Phase 3 依賴 Phase 2 全部模組，正確
- Phase 4 依賴 Phase 3（dashboard 需要 StreamProcessor），正確
- Phase 5 依賴 Phase 4（框架已建立）+ Phase 1（KnowledgeBase, FeedbackStore），正確

### 2.2 缺失的元件（⚠️）

以下 specs 定義的功能/元件在計畫中缺少偽代碼或明確規劃：

| 缺失項 | 出處 | 嚴重度 |
|--------|------|--------|
| `audio_utils.py` | [[system_overview#9. 目錄結構]] | 低 — 可在 Phase 1 內補充 |
| `StatusBar` 元件 | [[ui_spec#6. 狀態列]] | 中 — 已提及但無偽代碼 |
| 會議資訊對話框 | [[ui_spec#2.2]] | Major — 見 §3 |
| 頂部列元件 | [[ui_spec#2.3 頂部列（會中）]] | 中 — 已提及但無偽代碼 |

---

## 3. 品質：偽代碼邏輯審查

### 🔴 Critical-1：AudioRecorder 串流單位錯誤

**位置：** Phase 1-3，`AudioRecorder.start()` 方法

**問題：** 計畫使用 `self.chunk_duration = config.get("streaming.audio_chunk_duration_sec")` 作為 async generator 的 yield 間隔。依 [[data_schema#8. 設定檔]]，此值為 `600` 秒（10 分鐘）。

這意味著 AudioRecorder 要累積 **10 分鐘** 音訊才 yield 一次給 Transcriber。但 [[system_overview#4.1 即時串流架構]] 的核心承諾是「**錄音同時轉錄**」、「**即時逐字稿持續產出，UI 即時滾動顯示**」。

計畫**混淆了兩個不同概念**：

| 概念 | 用途 | 合理間隔 |
|------|------|---------|
| 音訊**暫存分段** | 每 10 分鐘切段存 WAV 到 `data/temp/`，管理磁碟資源 | 600 秒 |
| 串流**轉錄單位** | 送給 Whisper 即時轉錄，結果即時送 UI | 5~15 秒 |

**影響：** 若不修正，使用者開始錄音後要等 10 分鐘才能看到第一行逐字稿。**根本性地破壞產品「即時」定位。**

**修正建議：**
1. AudioRecorder 應以小區塊（如 5~10 秒）yield 音訊給 StreamProcessor/Transcriber
2. 同時在背景累積，每 10 分鐘寫一次暫存 WAV（`audio_chunk_duration_sec` 控制此行為）
3. 在 `config/default.yaml` 新增 `streaming.transcribe_chunk_sec`（如 10 秒），與 `audio_chunk_duration_sec` 分離
4. 或：AudioRecorder 只管串流小區塊，WAV 暫存寫入邏輯由 StreamProcessor 或獨立 buffer 負責

### 🟡 Major-1：Exporter 自動刪除音檔 vs UI 確認

**位置：** Phase 2-4，`Exporter.export()` 方法（計畫第 528-531 行）

**問題：** 計畫中 Exporter 在匯出後**直接刪除暫存音檔**。但 [[ui_spec#7. 對話框與通知]] 明確定義：

> 匯出成功 → 對話框 →「匯出完成。是否刪除原始音檔？」[刪除] [保留]

**修正建議：** Exporter 只負責匯出 Markdown，不主動刪除音檔。音檔刪除由 UI 層在使用者確認後呼叫。

### 🟡 Major-2：會議資訊對話框缺失

**位置：** Phase 4-2，`DashboardView`

**問題：** [[ui_spec#2.2]] 定義了開始錄音/匯入時的「會議資訊」快速填寫對話框（會議名稱 + 與會人員），包含 [開始] 和 [跳過] 選項。計畫的 DashboardView 偽代碼完全未涵蓋此流程。

**影響：** 使用者無法在開會前填寫會議資訊和與會人員，影響 Gemma 的摘要品質（缺少參與者上下文）。

### 🟡 Major-3：決議區塊在 Dashboard 中缺少獨立呈現

**位置：** Phase 4-2，`DashboardView._build_live_dashboard()`

**問題：** [[ui_spec#2.4]] 明確定義「決議事項獨立區塊呈現，不混在摘要文字中」。但計畫只有 `HighlightsPanel`（重點）和 `ActionsPanel`（Actions），decisions 沒有獨立處理。`SummaryResult.decisions` 在 UI 上如何呈現完全未規劃。

**修正建議：** 在 HighlightsPanel 中新增 decisions 子區塊，或將其重命名為 `SummaryPanel` 以涵蓋 highlights + decisions。

### 🟡 Major-4：會中 Action Items 保護機制不足

**位置：** Phase 4-2，`DashboardView.on_new_summary()`

**問題：** [[ui_spec#2.3]] 和 [[data_schema#5.1]] 規定「使用者手動修改的內容會被保護，AI 週期更新只影響未被手動修改的部分」。計畫對 highlights 有 `_user_edited_highlights` 檢查，但對 Action Items 只寫了 `self.actions_panel.merge(summary.action_items)` — 沒有說明如何判斷哪些 Action Items 是使用者手動修改/新增的、merge 時如何保護它們。

**修正建議：** 每個 ActionItem 需有 `user_edited: bool` 標記（或由 UI 層維護一個 edited_item_ids set），merge 時跳過已編輯項目。此涉及 data_schema，建議回報大統領評估是否需要更新 specs。

### 🔵 Minor-1：Summarizer 增量切片方式脆弱

**位置：** Phase 2-2，`Summarizer.generate()`，第 417 行

**問題：** `segments[previous_summary.covered_until:]` 用 `covered_until`（segment 全局序號）直接作為 list index。若 segment 有間隙（如某些 chunk 轉錄失敗導致跳號），切片會錯位。

**修正建議：** 改用 `[s for s in segments if s.index > previous_summary.covered_until]`。

### 🔵 Minor-2：階層式摘要僅有 stub

**位置：** Phase 2-2，`Summarizer._check_hierarchical_needed()`

**問題：** [[system_overview#4.4]] 詳細定義了階層式摘要的段落分群與合併策略。計畫只列出方法名稱，無任何邏輯。

**影響：** 低。短中期會議不需要此功能，但長期需補完。建議在 Phase 2 自測時標記為 TODO。

### 🔵 Minor-3：虛擬捲動未提及

**位置：** Phase 4-2

**問題：** [[system_overview#4.3]] 提到「UI 逐字稿：虛擬捲動，只渲染可見區域」。計畫未提及實作策略。

**影響：** 短會議無感，但 2 小時以上的會議可能造成 UI 卡頓。

---

## 4. 自驗證計畫

### 4.1 覆蓋度（✅ 良好）

每個 Phase 都有對應的自測方式，涵蓋：
- Phase 0：單元測試 + 骨架驗證
- Phase 1：各模組獨立單測/整合測試
- Phase 2：模組串接整合測試
- Phase 3：端對端整合測試（30 秒音檔完整跑 Pipeline）
- Phase 4-5：手動測試

### 4.2 遺漏的測試場景

| 遺漏 | 嚴重度 | 建議 |
|------|--------|------|
| **Summarizer JSON 解析失敗** — 風險矩陣列為中風險，但自測計畫未包含「Gemma 回傳非 JSON 時的 fallback」驗證 | 中 | Phase 2 加入 mock 非 JSON 回應的測試 |
| **Session 持久化 → 重新載入**：SessionManager 有 `save/load`，但自測只測生命週期流轉，未測「save 後 load 回來資料一致」 | 中 | Phase 2 加入 save→load 迴圈驗證 |
| **會中編輯保護**：核心差異化功能，但無任何自測項 | 高 | Phase 4 加入：「使用者編輯 highlights → 觸發 on_new_summary → 驗證使用者版本未被覆蓋」 |
| **音檔匯入 + Pipeline** 端對端 | 低 | Phase 3 已測 AudioImporter 路徑，足夠 |

---

## 5. 風險矩陣

### 5.1 覆蓋度（✅ 良好）

高/中/低三級分類合理，核心風險（CPU 推理慢、記憶體、格式不穩定、RAG 誤判）均已識別。應對策略具體可行。

### 5.2 遺漏的風險

| 遺漏風險 | 建議等級 | 說明 |
|----------|---------|------|
| **Whisper + Gemma 同時推理的 CPU 競爭** | 中 | 兩個模型同時跑 CPU 推理，可能互相拖慢。計畫只分別評估了記憶體，但未考慮 CPU 時間片競爭。建議：Gemma 摘要設為低優先級 thread，或確保串行（不在 Whisper 忙碌時啟動摘要）。 |
| **sounddevice 跨平台相容性** | 低 | Windows 上可能需要特定 audio backend（WASAPI/DirectSound）。建議 Phase 1 自測時記錄 Windows 環境注意事項。 |

---

## 6. 總結

### 必須修正（Re-review 前）

| # | 等級 | 摘要 |
|---|------|------|
| C-1 | 🔴 Critical | AudioRecorder 串流單位應為 5~10 秒轉錄區塊，非 10 分鐘暫存區塊 |
| M-1 | 🟡 Major | Exporter 不應自動刪除音檔，需由 UI 層確認後執行 |
| M-2 | 🟡 Major | 補充會議資訊對話框流程（[[ui_spec#2.2]]） |
| M-3 | 🟡 Major | 補充決議區塊在 Dashboard 中的獨立呈現方式 |
| M-4 | 🟡 Major | 說明 Action Items 的 merge 保護機制 |

### 建議改善（可延後）

| # | 等級 | 摘要 |
|---|------|------|
| m-1 | 🔵 Minor | Summarizer 增量切片改為 index 過濾而非 list 切片 |
| m-2 | 🔵 Minor | 階層式摘要補充偽代碼（至少骨架邏輯） |
| m-3 | 🔵 Minor | 標註虛擬捲動策略 |
| T-1 | 📋 自測 | 加入 Summarizer JSON fallback 測試 |
| T-2 | 📋 自測 | 加入 SessionManager save→load 一致性測試 |
| T-3 | 📋 自測 | 加入會中編輯保護測試 |
| R-1 | ⚠️ 風險 | 補充 Whisper + Gemma CPU 競爭風險 |

---

## 審察者簽章

- **審察者**：CLI-2
- **日期**：2026-04-05
- **審查輪次**：第 1 次
- **判定**：❌ Fail — 修正 Critical + Major 後 Re-review

---
---

# 第 2 輪 Re-review（2026-04-05）

> 碼農已修正計畫（status: revised），Specs 同步更新（`ActionItem.user_edited`、`streaming.transcribe_chunk_sec`）。
> 逐項驗證第 1 輪提出的 5 項必須修正 + 6 項建議改善。

---

## 判定：✅ Pass

**理由：** 5 項必須修正全部正確處理，6 項建議改善中 5 項已處理、1 項可接受延後。計畫與 specs 一致，偽代碼邏輯合理，自驗證覆蓋完整。可進入 S Phase。

---

## 1. 必須修正項逐項驗證

### C-1：AudioRecorder 串流分兩層 — ✅ 通過

**修正內容：**
- AudioRecorder（計畫第 234~311 行）改為雙層設計：
  - 層 1：每 `transcribe_chunk_sec`（10 秒）yield 小區塊給 Transcriber — 即時轉錄
  - 層 2：每 `save_chunk_sec`（600 秒）背景暫存 WAV — 磁碟管理
- 雙 buffer（`transcribe_buffer` + `save_buffer`）獨立運作，finally 中兩者分別 flush
- AudioImporter（第 315~351 行）同步修正，yield 單位改用 `transcribe_chunk_sec`
- StreamProcessor（第 615~616 行）註釋更新：「每 10 秒一個小區塊」
- Specs 同步：`config/default.yaml` 新增 `streaming.transcribe_chunk_sec: 10`，與 `audio_chunk_duration_sec: 600` 明確分離

**審查意見：** 雙層分離乾淨，即時轉錄承諾得以實現。10 秒為合理的轉錄區塊長度（faster-whisper small 在 CPU 上處理 10 秒音訊約需 3~5 秒，有足夠餘裕）。

### M-1：Exporter 不自動刪音檔 — ✅ 通過

**修正內容：**
- Exporter（第 571~607 行）：`export()` 只寫 Markdown + 更新 status/export_path，不再呼叫 `unlink`
- 音檔刪除邏輯移至 `SessionManager.delete_audio()`（第 560~564 行），由 UI 層在使用者確認後呼叫
- DashboardView（第 822 行）標註匯出後彈確認對話框

**審查意見：** 職責分離正確，符合 [[ui_spec#7. 對話框與通知]] 的「匯出完成。是否刪除原始音檔？」流程。

### M-2：會議資訊對話框 — ✅ 通過

**修正內容：**
- 新增 `_show_meeting_info_dialog()`（第 753~790 行）：
  - 名稱欄位（預設 `YYYY-MM-DD HH:MM 會議`）
  - 與會人員欄位（逗號分隔，轉為 `Participant(source="manual")`）
  - [開始] 觸發 `on_confirm(title, participants)`
  - [跳過] 使用預設值
- `_on_start_recording()`（第 792~797 行）和 `_on_import_audio()`（第 799~802 行）都先彈對話框再啟動 Pipeline

**審查意見：** 完整涵蓋 [[ui_spec#2.2]] 定義的流程。與會人員的 `source="manual"` 正確對應 [[data_schema#5. Session]] 中 Participant.source 欄位。

### M-3：決議區塊獨立呈現 — ✅ 通過

**修正內容：**
- `HighlightsPanel` 重構為 `SummaryPanel`（第 846~864 行），包含：
  - `highlights_editor`：會議重點（TextField）
  - `decisions_list`：決議事項獨立區塊（Column），以 `ft.Divider()` 分隔
- `on_new_summary()`（第 829~843 行）分別更新 highlights 和 decisions，各自有 `_user_edited_*` 保護

**審查意見：** 決議與重點分離呈現，符合 [[ui_spec#2.4]] 「決議事項獨立區塊呈現，不混在摘要文字中」的要求。

### M-4：Action Items merge 保護 — ✅ 通過

**修正內容：**
- `ActionItem.user_edited: bool` 新增於 models.py（第 108 行），specs `data_schema` 已同步更新（第 125 行）
- `ActionsPanel.merge_with_protection()`（第 870~891 行）策略明確：
  1. `user_edited=True` → 跳過不覆蓋
  2. `user_edited=False` 且 AI 有更新 → 更新內容
  3. 新 AI 項目 → 新增（`user_edited=False`）
  4. 舊項目不在新 AI 結果中 → 保留（不主動刪除）
- StreamProcessor 中 `_summarizing` 旗標與 merge 邏輯配合

**審查意見：** 保護策略完整合理。第 4 點「不主動刪除」是正確的保守決策 — 避免 AI 漏產出時誤刪使用者尚未標記的項目。

---

## 2. 建議改善項驗證

| # | 項目 | 狀態 | 驗證說明 |
|---|------|------|---------|
| m-1 | Summarizer 增量切片改 index 過濾 | ✅ 已修正 | 第 460 行改為 `[s for s in segments if s.index > previous_summary.covered_until]` |
| m-2 | 階層式摘要偽代碼 | ⏸️ 未處理 | `_check_hierarchical_needed` 仍為 stub。**可接受** — 短期會議不需要，風險矩陣已標註應對策略 |
| m-3 | 虛擬捲動 | ⏸️ 未處理 | **可接受** — Flet 的 `ListView` 預設即為虛擬捲動，實作時自然採用，不需要在計畫層級特別設計 |
| T-1 | Summarizer JSON fallback 測試 | ✅ 已加入 | Phase 2 驗證（第 1005 行）：mock 非 JSON 回應，驗證 fallback |
| T-2 | SessionManager save→load 測試 | ✅ 已加入 | Phase 2 驗證（第 1006 行）：save→load 迴圈驗證欄位一致性 |
| T-3 | 會中編輯保護測試 | ✅ 已加入 | Phase 4-5 驗證（第 1020 行）：highlights + ActionItem 雙重保護測試 |
| R-1 | Whisper + Gemma CPU 競爭風險 | ✅ 已加入 | StreamProcessor `_summarizing` 旗標（第 631、668 行）+ 風險矩陣新增條目（第 1045 行） |

---

## 3. 新增觀察（非阻塞）

### 觀察 1：StreamProcessor 摘要期間轉錄暫停

`StreamProcessor.run()` 中摘要使用 `await`（第 670 行），在等待 Gemma 回應期間 `async for` 迴圈暫停，AudioRecorder 的音訊會堆積在 `_audio_queue` 中。Gemma 4B Q4 在 CPU 上推理約 15~30 秒，期間約 2~3 個 10 秒轉錄區塊被延遲。

**評估：** 可接受。音訊不會丟失（queue 持續收集），摘要完成後轉錄會快速追趕。使用者感受為「摘要更新後逐字稿短暫快速補上幾行」。若未來發現體驗不佳，可改為 `asyncio.create_task` 平行化（風險矩陣第 1045 行已記錄此備案）。

### 觀察 2：`_summarizing` 旗標在單線程 async 中為冗餘

由於 `await` 天然串行化，`_summarizing` 旗標在當前設計中不會被並發讀寫。但作為意圖文件和未來平行化的預留，保留無害。

---

## 4. 總結

| 類別 | 結果 |
|------|------|
| 必須修正（5 項） | ✅ 全部通過 |
| 建議改善（7 項） | 5 項已處理、2 項可接受延後 |
| Specs 同步 | ✅ `data_schema` 已更新 `ActionItem.user_edited`、`streaming.transcribe_chunk_sec` |
| 新引入問題 | 無阻塞性問題 |

**計畫品質評估：** 分期合理、依賴正確、偽代碼邏輯清晰、自驗證覆蓋核心路徑、風險矩陣務實可行。符合進入 S Phase 的條件。

---

## 審察者簽章

- **審察者**：CLI-2
- **日期**：2026-04-05
- **審查輪次**：第 2 次
- **判定**：✅ Pass — 計畫可進入 S Phase
